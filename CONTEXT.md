# Context

## Glossary

### Main Worktree

The primary worktree for a Git repository. It is the directory `gwt` returns to
from a linked worktree.

### Linked Worktree

An additional Git worktree associated with the same repository as the main
worktree. A linked worktree may be created by `gwt` or by Git directly.
